<% if @tags %>
<div class="module">
	<h3>Tags</h3>
	<ul>
	<% @tags.each do |tag| %>
		<li><%= link_to tag.name, :controller => 'posts', :action => 'tag', :id => tag.name %></li>
	<% end %>
	</ul>
</div>
<% end %>